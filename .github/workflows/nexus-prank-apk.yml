name: Nexus Prank APK

on:
  push:
    branches:
      - nexus-prank-apk
    paths:
      - 'nexus-prank-android/**'
      - '.github/workflows/nexus-prank-apk.yml'
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android platform
        run: sdkmanager 'platforms;android-35' 'build-tools;35.0.0'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build debug APK
        working-directory: nexus-prank-android
        run: gradle :app:assembleDebug --stacktrace --no-daemon

      - name: Verify APK
        run: |
          set -euo pipefail
          APK='nexus-prank-android/app/build/outputs/apk/debug/app-debug.apk'
          test -s "$APK"
          mkdir -p nexus-prank-dist
          cp "$APK" nexus-prank-dist/Nexus-Prank-v1.0.0.apk
          "$ANDROID_SDK_ROOT/build-tools/35.0.0/aapt" dump badging "$APK" | tee nexus-prank-dist/apk-badging.txt
          sha256sum nexus-prank-dist/Nexus-Prank-v1.0.0.apk | tee nexus-prank-dist/SHA256SUMS.txt

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Nexus-Prank-APK
          path: nexus-prank-dist/
          if-no-files-found: error
          retention-days: 14
