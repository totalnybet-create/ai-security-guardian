name: Runner Smoke

on:
  pull_request:
  push:
    branches:
      - 'work/**'

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Runner started
        run: echo "RUNNER_OK"
